# lekha-task-6
This code uses the Iris dataset to classify flower species based on petal size using the K-Nearest Neighbors algorithm. It scales the data, tests different K values, shows accuracy and confusion matrices, and visually plots decision boundaries to understand how well the model separates the classes.
